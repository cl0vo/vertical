[project]
name = "arara-factory"
version = "0.17.3"
description = "Guided ARARA Factory with resumable YouTube uploads, persistent diagnostics and safe confirmed close"
requires-python = ">=3.11"
dependencies = [
  "numpy>=1.26",
  "Pillow>=10.4",
  "pydantic>=2.7",
  "PySide6>=6.7",
  "typer>=0.12",
  "pyyaml>=6.0",
  "requests>=2.32",
  "google-api-python-client>=2.170",
  "google-auth>=2.40",
  "google-auth-httplib2>=0.2",
  "google-auth-oauthlib>=1.2",
]

[project.scripts]
arara-factory = "arara_factory.entry:main"
arara-factory-gui = "arara_factory.entry:main"
arara-factory-batch = "arara_factory.entry:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
