[project]
name = "arara-factory"
version = "0.11.1"
description = "Personal Windows app for CapCut-style ARARA reels and resumable hour-long batches"
requires-python = ">=3.11"
dependencies = [
  "numpy>=1.26",
  "Pillow>=10.4",
  "pydantic>=2.7",
  "PySide6>=6.7",
  "typer>=0.12",
  "pyyaml>=6.0",
]

[project.scripts]
arara-factory = "arara_factory.cli:app"
arara-factory-gui = "arara_factory.app:main"
arara-factory-batch = "arara_factory.batch_app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
